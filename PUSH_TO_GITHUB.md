# 📤 Як відправити код на GitHub (NCobrimark/Archetype_UA)

Привіт! Оскільки у мене немає доступу до твого GitHub-акаунту, я не можу сам натиснути кнопку "Відправити". Але я підготував для тебе **всі команди**.

Тобі треба просто скопіювати їх і виконати на своєму комп'ютері.

### Крок 1: Відкрий термінал
Переконайся, що ти знаходишся в папці `archetype_bot`.

### Крок 2: Скопіюй і встав ці команди (по одній)

1. **Створюємо сховище:**
   ```bash
   git init
   ```

2. **Додаємо всі файли:**
   ```bash
   git add .
   ```

3. **Зберігаємо зміни:**
   ```bash
   git commit -m "Запуск Archetype Bot 1.0"
   ```

4. **Називаємо головну гілку:**
   ```bash
   git branch -M main
   ```

5. **Підключаємо твоє посилання (GitHub):**
   ```bash
   git remote add origin https://github.com/NCobrimark/Archetype_UA.git
   ```
   *(Якщо напише "remote origin already exists", введи: `git remote set-url origin https://github.com/NCobrimark/Archetype_UA.git`)*

6. **Відправляємо в Інтернет 🚀:**
   ```bash
   git push -u origin main
   ```

Коли ти введеш останню команду, комп'ютер може запитати твоє **ім'я** та **пароль** від GitHub. Введи їх, і код полетить на сайт!
