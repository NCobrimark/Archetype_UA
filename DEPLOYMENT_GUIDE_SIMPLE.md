# 🚀 Як запустити твого бота в Інтернет (Інструкція "Для Своїх")

Привіт! Уяви, що твій комп'ютер —ші це дім, де живе твій бот. Але ми хочемо, щоб він жив у "хмаринці" (в Інтернеті), де він буде доступний завжди, навіть коли ти спиш.

Для цього нам треба зробити дві речі:
1. **GitHub** — це як бібліотека, куди ми покладемо "книгу" з кодом твого бота.
2. **Railway** — це "чарівний будиночок" в Інтернеті, який візьме цю книгу з бібліотеки і оживить бота.

---

## Крок 1: Відправляємо код на GitHub (У Бібліотеку)

### 1. Створи "Поличку" на сайті
1. Зайди на сайт [GitHub.com](https://github.com) і увійди в свій акаунт.
2. Знайди кнопку **"New"** (або зелену "Create repository") зліва або зверху.
3. У полі **Repository name** напиши назву, наприклад: `my-archetype-bot`.
4. Більше нічого не чіпай. Натисни зелену кнопку **"Create repository"**.
5. **Важливо!** Ти побачиш сторінку з купою коду. Знайди блок, де є заголовок **"…or create a new repository on the command line"**. Не закривай цю сторінку, вона нам зараз знадобиться!

### 2. "Запаковуємо" код на твоєму комп'ютері
Тепер нам треба сказати твоєму комп'ютеру відправити файли туди.

1. Відкрий термінал (або командний рядок) прямо в папці з ботом (`archetype_bot`).
2. Вводь ці команди по черзі (після кожної натискай Enter):

   - **Створюємо "магічну книгу":**
     ```bash
     git init
     ```
   - **Додаємо всі сторінки (файли) в книгу:**
     ```bash
     git add .
     ```
   - **Підписуємо, що це перша версія:**
     ```bash
     git commit -m "Мій перший бот"
     ```
   - **Називаємо головну гілку "main":**
     ```bash
     git branch -M main
     ```
   - **Вказуємо адресу твоєї полички (ТУТ ТРЕБА ТВОЄ ПОСИЛАННЯ!):**
     *Повернися на сайт GitHub, скопіюй рядок, який починається на `git remote add origin https://...` і встав його сюди.*
     ```bash
     git remote add origin https://github.com/TVІЙ_НIК/my-archetype-bot.git
     ```
   - **Відправляємо книгу в бібліотеку:**
     ```bash
     git push -u origin main
     ```

Якщо все добре, комп'ютер напише щось про "writing objects" і "100%". Вітаю, твій код тепер в Інтернеті!

---

## Крок 2: Оживляємо бота на Railway (Будиночок)

Тепер найцікавіше. Поселимо бота.

1. Зайди на сайт [Railway.app](https://railway.app/) і увійди (можна через той самий GitHub).
2. Натисни велику кнопку **"New Project"**.
3. Вибери пункт **"Deploy from GitHub repo"**.
4. Знайди у списку свій `my-archetype-bot` (який ми щойно створили) і натисни на нього.
5. Натисни **"Deploy Now"**.

Railway почне будувати будиночок (ти побачиш кружечок, що крутиться). Але бот ще не працює, бо він не знає секретних паролів!

### 3. Даємо боту секретні ключі (Variables)
Бот потребує ключів, щоб відкривати двері до Telegram та AI.

1. У своєму проекті на Railway натисни на прямокутник з назвою твого бота.
2. Зверху вибери вкладку **"Variables"**.
3. Натисни **"New Variable"** і додай їх по черзі (точно так само, як у твоєму файлі `.env` на комп'ютері):

| Назва (KEY) | Значення (VALUE) |
|---|---|
| `BOT_TOKEN` | Твій токен від BotFather (цифри і букви) |
| `OPENROUTER_API_KEY` | Твій ключ від OpenRouter (`sk-or-...`) |
| `OPENROUTER_MODEL` | `google/gemini-2.0-flash-exp:free` |
| `DATABASE_URL` | `sqlite+aiosqlite:///./archetype.db` (це тимчасова база, для початку підійде) |

4. Після того як додаси останній ключ, Railway **автоматично перезапустить** бота.

### Як перевірити?
Натисни на вкладку **"Logs"**. Якщо ти бачиш там напис "Starting Bot Polling...", значить твій бот живий! Біжи в Telegram і пиши йому `/start`.

Ти крутий розробник/розробниця! 🥳
